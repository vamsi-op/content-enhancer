{
  "name": "content-audit-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist/* .",
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
